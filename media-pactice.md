
# **1) ใช้ max-width แต่วางลำดับผิด → กฎถูก override ผิดตัว**

ตัวอย่างผิด:

```css
@media (max-width:1024px) { ... }
@media (max-width:1440px) { ... }   /* อยู่หลัง → มัน override ตัว 1024 */
```

ทำให้:

* มือถือถูกใช้กฎของ desktop
* layout พังแบบงง ๆ

### ✔ วิธีที่ถูก:

เรียงจาก “ใหญ่ → เล็ก” หรือ “เล็ก → ใหญ่” ให้ *สม่ำเสมอ*

**ถ้าพี่ใช้ max-width ให้เรียงจากใหญ่สุด → เล็กสุด**

```css
@media (max-width:1440px) { ... }
@media (max-width:1024px) { ... }
@media (max-width:768px)  { ... }
@media (max-width:480px)  { ... }
```

---

# **2) ลืมว่ากฎด้านล่าง override ด้านบนเสมอ**

CSS มีลำดับ → ของล่างสุดชนะ
ถ้า breakpoint ซ้อนกัน ก็ต้องคุมความหมายให้ดีว่าอันไหนควรอยู่ท้าย

---

# **3) อย่าตั้ง width แบบตายตัวใน @media ถ้าไม่จำเป็น**

ตัวอย่างที่ทำให้ layout พังบ่อย:

```css
.card { width: 300px; }
```

พอเข้า mobile → มันใหญ่เกินหน้าจอ

### ✔ ใช้แบบนี้:

```css
.card { width: 100%; max-width: 300px; }
```

หรือให้ flex/grid คุมความกว้างแทน

---

# **4) ใช้ padding/margin ใหญ่เกินบน mobile**

เช่น:

```css
section {
  padding: 6rem;
}
```

บนมือถือ = หน้าเว็บเหลือพื้นที่ให้เนื้อหานิดเดียว

### วิธีแก้:

```css
@media (max-width:480px) {
  section { padding: 2rem 1rem; }
}
```

---

# **5) อย่าซ่อน element ด้วย visibility ถ้าไม่จำเป็น**

```css
visibility:hidden;
```

มันยังคงกินพื้นที่
ใช้:

```css
display:none;
```

ถ้าต้องการซ่อนจริง ๆ ใน layout mobile

---

# **6) ระวังข้อความยาวไม่ขึ้นบรรทัดบนมือถือ**

เพราะบางคนใส่:

```css
white-space: nowrap;
```

แล้วลืม reset ตอน mobile

### วิธีแก้:

```css
@media(max-width:480px){
  .text-block { white-space: normal; }
}
```

---

# **7) รูปใหญ่เกิน viewport → ต้องใช้ object-fit หรือ max-width**

เพื่อป้องกันรูปทะลุจอ:

```css
img {
  width: 100%;
  height: auto;
  object-fit: cover;
}
```

---

# **8) background-position ต้องปรับตาม mobile**

เพราะภาพจะถูก crop ต่างกันระหว่าง desktop/mobile

ตัวอย่างที่ต้องปรับ:

```css
@media(max-width:768px){
  .hero {
    background-position: center top;
  }
}
```

ไม่งั้น hero หัวขาด / ตัวหาย

---

# **9) ระวัง flex-direction เปลี่ยน แต่ยังใช้ margin แบบเดิม**

ตัวอย่าง:

Desktop → row
Mobile → column

แต่ margin ยังเป็นแบบ desktop:

```css
.card { margin-right: 3rem; } /* ผิดบน mobile */
```

ต้องเปลี่ยนเป็น:

```css
@media(max-width:768px){
  .card { margin-right: 0; margin-bottom: 1rem; }
}
```

---

# **10) ควรมี “base style” ที่สวยก่อน แล้วค่อย override**

ไม่ควรทำแบบ:

```css
/* ทำให้ mobile สวยก่อน */
@media(max-width:480px){ ... }

/* desktop ค่อย override เยอะมาก */
```

มันทำให้ซับซ้อนและแก้ยาก เพราะ desktop มักมี layout ที่ใหญ่กว่า

### Best practice ที่อ่านง่ายที่สุด:

**เขียน desktop base ก่อน → ค่อยลดระดับให้ tablet → mobile**

---

# 🌈 Checklist ก่อนเริ่มเขียน @media (ช่วยลดปัญหา 90%)

### ✔ เรียงลำดับ breakpoint ให้ถูก

1440 → 1024 → 768 → 480

### ✔ ทุก element ควรเขียน “base responsive” ก่อน

เช่นรูป = width:100%;
container = max-width + padding

### ✔ ใช้ flex/grid มากกว่า fixed width

เพราะมันขยาย-หดตามจอเอง

### ✔ ใช้ gap แทน margin ถ้าได้

ทำให้ responsive ง่ายขึ้นมาก

### ✔ ทดสอบบน 4 ความกว้างทันทีที่เขียน

ใน DevTools:
375px (mobile)
480px (mini)
768px (tablet)
1024px (laptop)
1440px (desktop)

### ✔ อย่าให้ breakpoint ทำสไตล์ขัดกันเอง

เช่น mobile บอก font-size = 20px
tablet บอก 18px
desktop บอก 16px
→ เขียนให้เป็น pattern เดียวกัน เช่นลดลงอย่างเป็นขั้นบันได

